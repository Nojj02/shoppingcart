package com.jll.models.cartModel;

import com.jll.models.Identity;

import java.util.UUID;

public class CartIdentity extends Identity {
    public CartIdentity(UUID value) {
        super(value);
    }
}
