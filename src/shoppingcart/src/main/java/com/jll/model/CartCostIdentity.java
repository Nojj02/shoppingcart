package com.jll.model;

import com.jll.model.Identity;

import java.util.UUID;

public class CartCostIdentity
    extends Identity {
    protected CartCostIdentity(UUID value) {
        super(value);
    }
}
